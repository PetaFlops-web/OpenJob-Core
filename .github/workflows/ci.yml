name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: "24"

jobs:
  # ── API: lint + test ────────────────────────────────────
  api:
    name: "API – Lint & Test"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - run: npm install --ignore-scripts
      - run: npm run lint
      - run: npm run test

  # ── UI: lint ────────────────────────────────────────────
  ui:
    name: "UI – Lint"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - run: npm install --ignore-scripts
      - run: npm run lint

  # ── API Docker build check ──────────────────────────────
  api-docker:
    name: "API – Docker Build"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          grep -v '^Dockerfile$' api/.dockerignore > /tmp/di && mv /tmp/di api/.dockerignore
          docker build -f api/Dockerfile api

  # ── UI Docker build check ───────────────────────────────
  ui-docker:
    name: "UI – Docker Build"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          grep -v '^Dockerfile$' ui/.dockerignore > /tmp/di && mv /tmp/di ui/.dockerignore
          docker build -f ui/Dockerfile ui